import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 24,
    top: -20,
    gap: 30
  },
  separator: {
    backgroundColor: '#808080',
    height: 0.4,
    width: '100%',
  },
  emptyI: {
    color: '#808080',
    textAlign:  'center',
    fontSize: 18,
    fontWeight: 'bold',
  },
  emptyII: {
    color: '#808080',
    textAlign:  'center',
    fontSize: 18,
    fontWeight: '300',
    top: -25
  }
});